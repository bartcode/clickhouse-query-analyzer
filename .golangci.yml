version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell

issues:
  max-issues-per-linter: 50
  max-same-issues: 10
